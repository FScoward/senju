---
name: kouunryuusui
license: MIT
description: "Epic/USチケットから設計→チケット分割→実装→PR作成まで自律的に走り続ける2層構造の統合開発ワークフロー。上位フロー（設計・分割）と下位フロー（実装・PR）を分離し、E&Cも設計に基づいて整理する。明示的に「/kouunryuusui」と指示された場合のみ使用。"
aliases:
  - kurs
---

# 行雲流水（kouunryuusui） $ARGUMENTS

Epic/USチケットを起点に、**設計→チケット分割→実装→PR作成**まで自律的に走り続ける開発ワークフロー。

## 前提スキル（要インストール）

このスキルは以下のスキルがインストールされていることを前提とする。

| スキル | 用途 | 使用フェーズ |
|-------|------|------------|
| `software-requirements` | 対話形式での要求整理 | E1前（任意） |
| `sier-dev` | 要件定義書〜内部設計書の生成 | E1（仕様書なし時） |
| `managing-database-migrations` | DBマイグレーションファイル作成 | T1 |

## 関連スキル（チケット情報収集）

以下のスキルはチケット内容が不十分な場合（E1 Step 1a / T0 ステップ0）で使用する。

| スキル | 用途 | 使用フェーズ |
|-------|------|------------|
| `jira-cli` | 親チケット・Epic・リンクチケットの取得 | E1 Step 1a・T0（チケット内容不十分時）|

## 関連スキル（PR分割・サイズ管理）

以下のスキルは必須ではないが、E3（チケット分割）・T5（Push確認）で参照することを推奨する。
PRを小さく保つための道具として本フローに統合する。

| スキル | 用途 | 使用フェーズ |
|-------|------|------------|
| `feature-flag-strategy` | 機能追加を5PR（Add/BE/FE/Enable/Remove）に段階分割 | E3（新機能追加時）|
| `vertical-slice` | Walking Skeleton + 段階拡張で機能を縦スライス化 | E3（機能追加時の代替戦略）|
| `tidy-first` | 整理（Tidying）を本体実装の前に先行PRに切り出す | E3（既存修正時）|
| `data-model-designer` | T字形ER技法と Expand & Contract によるDBスキーマ設計 | E2c（DB変更あり時）|
| `stacked-pr` | 依存チェーン状PRの運用（base指定・rebase伝播等）| E4（Team ワーカーのPR作成）|
| `pr-size-guard` | PR push直前の差分サイズ測定と分割判定 | T5（Push確認の直前）|
| `split-on-the-fly` | 実装後に「大きすぎる」と気づいた時の事後分割手順 | T5（`pr-size-guard` で Red の時）|
| `mihari` | テスト充足性を3並列レビュー×収束ループで検証 | QG-3 Stage1 パートB（テスト充足性ループ）|
| `simplify` | 変更コードを reuse / quality / efficiency 観点でレビュー＆自動修正 | QG-2（コード品質改善）|

## アーキテクチャ概要

```
kouunryuusui
│
├─ 入力判定: Epic/US チケット or 実装チケット?
│   ├─ Epic/US → 上位フロー（設計→分割→下位フローをループ）
│   └─ 実装チケット → 下位フロー（実装→PR）
│
├─ 上位フロー (Epic Level): E1〜E4
│   ├─ E1: Epic 取得 & AC品質チェック & AC生成（旧E1+E4統合）
│   │     → サブエージェントで完結、spec-draft.md に永続化
│   ├─ E2: アーキテクチャ設計（DB/API/UI/設計図）
│   │     → サブエージェントで完結、design.md に永続化
│   ├─ E3: チケット分割 & 依存グラフ生成
│   │     → サブエージェントで完結、ticket-plan.md に永続化
│   └─ E4: チケット実行ループ（Native Team オーケストレーション）
│       ├─ TeamCreate で全チケットをタスク登録
│       ├─ 依存順にワーカー（general-purpose）を並列起動
│       ├─ SendMessage でリアルタイム進捗共有・コンフリクト予防
│       ├─ 段階的エスカレーション（ワーカー→親→ユーザー）
│       └─ 一括Push承認 → TeamDelete
│
└─ 下位フロー (Ticket Level): T0〜T5
    ├─ T0: AC品質チェック & ブラッシュアップ（GWT+Examples形式・7項目網羅性）★スキップ禁止
    ├─ T1: Worktree 作成 & 隔離（Team ワーカーモード対応）
    ├─ T2: モック/スケルトン（UI変更時のみ）
    ├─ T3: 実装（TDD / Team並列、旧T3-preの検証基準定義を統合）
    ├─ QG: 品質ゲート（旧T3b+T4+T5統合、サブエージェント内で完結）
    │     → フォーマット→ビルド→レビュー(2並列)→修正ループ(max5)
    │     → メインに返すのは PASS/FAIL + サマリーのみ
    └─ T5: Push確認 & PR作成（唯一の停止点）
```

> **⚠️ 絶対ルール: 実装完了 ≠ タスク完了**
>
> T3（実装）が完了したら、**判断・確認なしで即座にQG（品質ゲート）を起動せよ。**
> 「PR作りますか？」「次のステップに進みますか？」と聞いてはならない。
> T3 → QG → T5 は一気通貫で実行する。停止点はT5のPush確認のみ。

## 核心原則

> **品質を最優先にせよ。時間をかけてでも正しく作れ。迷ったらDecision Recordを書いて進め。**

1. **品質は速度に優先する**: リファクタリングを省略しない。テストは境界値・異常系まで書く。コードレビュー指摘は全て対応する
2. **自律的に判断し、品質を落とさず進む**: 上位フロー E1〜E3 + 下位フロー T1〜QG は自律実行。選択肢がある場合はDecision Recordを作成し、最善の選択をして進む
3. **品質チェック失敗は必ず修正する**: QG内の自己修正ループで自動修正（最大5回）。ビルド通過だけでなくコード品質も担保する
4. **T5のみ停止**: ユーザーの明示的承認を待つ（pushは人間の判断）。T3完了後に「PR作りますか？」と聞いてはならない。T3→QG→T5は一気通貫
5. **ハーネスの定期見直し**: 各フェーズはモデルが単独では実行できない点についての仮定を符号化している。モデル改善により仮定が陳腐化する可能性を意識し、定期的にフローの各コンポーネントの必要性を見直す
6. **修正タスクでもフローを守る**: 既存コードへの修正・バグ修正・仕様変更でも全ステップを実行する。各ステップには修正時の固有の目的がある（影響調査、整合性チェック、回帰防止等）。「既に存在する」はスキップの理由にならない
7. **ACブラッシュアップは実装の前提条件**: GWT+Examples形式・7項目網羅性を満たしていないACで実装を始めてはならない。上位フロー（E1）を通らない下位フロー直接実行でも **T0（AC品質チェック）を必ず実施する**。形式不備のACをそのまま sprint-contract.md に転記しない
8. **作業根拠を明示する**: 各フェーズ完了時に「今どの情報を根拠に作業しているか」を `scratch.md` のSSOT宣言として記録する（E1完了→`spec-draft.md`、E2完了→`design.md`、E3完了→`ticket-plan.md`）。Jira/Notionとローカルで内容が食い違う場合は現フェーズのSSOTファイルを正とする

## advisor 使用ポリシー

> **advisor は「次の大きな決定を下す直前」に呼ぶ。設計後・実装後ではなく、設計前・分割前・Push前。**

`advisor` ツールはセッション全体のコンテキストを見る上位レビュアー。高リスクな判断ポイントで呼び出し、セカンドオピニオンを得る。

> **Tier 定義（`references/upper-flow.md` の「タスク性質判定」で決定）:**
> - **Tier 1**: CRUD・設定変更・既知パターン。変更ファイル3未満
> - **Tier 2**: 複数コンポーネント・UI・分岐多い。変更ファイル3〜15
> - **Tier 3**: 既存統合深い・パフォーマンス・ドメイン知識必要。変更ファイル15+ or 不明

### 呼び出しポイント（Tier 別）

| ポイント | Tier 1 | Tier 2 | Tier 3 |
|---------|--------|--------|--------|
| E1完了・E2開始前（AC妥当性） | スキップ | **呼ぶ** | **呼ぶ** |
| E2b完了・E3開始前（設計妥当性） | スキップ | スキップ | **呼ぶ** |
| E3完了・E4開始前（分割戦略） | スキップ | **呼ぶ** | **呼ぶ** |
| QG-4（根本原因分析） | 修正3回目以降 | 修正3回目以降 | **修正1回目から** |
| T5 Go/No-Go 前 | スキップ | **呼ぶ** | **呼ぶ** |

### advisor の呼び出し方

```
advisor()
# ※ advisor はパラメータ不要。現在の会話コンテキストを自動参照する。
# 呼び出し前に判断材料（設計文書・分割計画等）を会話コンテキストに展開しておくこと。
```

### advisor の応答の扱い方

- 具体的な懸念点が挙がった場合は、その点を修正してから次フェーズへ
- 「問題なし」の場合は次フェーズへ進む
- advisor の指摘とコードベース調査が矛盾する場合は、もう一度 advisor を呼んで reconcile を依頼する

---

### 品質原則（全フェーズ共通）

- **リファクタリングは省略禁止**: TDDのRefactorステップを飛ばさない。Green後に必ず重複排除・命名改善・責務分離を行う
- **テストは正常系だけでは不十分**: 異常系・境界値・エッジケースのテストも必須。ACに明記されていないエッジケースも自発的に追加する
- **コードレビュー指摘は全て対応**: Critical/Warningとも修正する。Infoは判断に基づき対応
- **「動けばいい」は品質ではない**: 可読性・保守性・テスト容易性を意識し、将来の開発者が理解しやすいコードを書く
- **急いで雑にするより、丁寧に一回で正しく作る**: 手戻りコストは初回の丁寧さより常に高い

## 並列実行の原則

- 独立した作業は常に `run_in_background: true` または複数Taskの同時起動で並列化する
- BE+FE両方の変更がある場合はClaude Code Native Team で並列実装する
- **E4の複数チケット並列実行**: Native Team（`TeamCreate` / `SendMessage`）でワーカーを起動し、リアルタイムメッセージングで進捗共有・コンフリクト予防・段階的エスカレーションを行う
- メインのコンテキストウィンドウをクリーンに保つため、各フェーズの実作業はサブエージェントに委譲する

## サブエージェントルーティング

| フェーズ | タスク | `subagent_type` | `model` |
|---------|--------|-----------------|---------|
| E1 | 仕様書→US+AC生成+品質チェック+補完 | `general-purpose` | Tier 1: haiku / Tier 2: sonnet / Tier 3: opus |
| E2 | 影響範囲調査（3並列） | `Explore` | haiku |
| E2 | アーキテクチャ設計（低〜中） | `Plan` | sonnet |
| E2 | アーキテクチャ設計（高） | `Plan` | opus |
| E2 | 設計図作成（状態遷移図・シーケンス図等、Mermaid.js） | — (設計エージェントが兼務) | — |
| E3 | チケット分割計画 + AC生成（SBE形式） | `general-purpose` | Tier 1: sonnet / Tier 2〜3: opus |
| E4 | Team オーケストレーション | `TeamCreate` + `SendMessage` | — |
| E4 | ワーカー起動 | `general-purpose` (team_name指定) | sonnet |
| T0 | AC品質チェック & ブラッシュアップ | `general-purpose` | Tier 1: haiku / Tier 2〜3: sonnet |
| T1 | マイグレーション | `general-purpose` | sonnet |
| T2 | モック/スケルトン生成 | `general-purpose` | sonnet |
| T3 | 実装（Team worker） | `general-purpose` | sonnet |
| T3 | テスト作成 | `general-purpose` | sonnet |
| QG | 品質ゲート全体（フォーマット→ビルド→レビュー→修正ループ） | `general-purpose` | sonnet |
| QG-3 Stage1 パートA | AC 適合チェック（Agent A 単独） | `general-purpose` | sonnet |
| QG-3 Stage1 パートB | **テスト充足性ループ**（`mihari` スキル呼び出し） | `mihari` スキル | — |
| QG-3 Stage2 Agent A | コードレビュー（ロジック、DDD、規約） | `general-purpose` | Tier 1〜2: sonnet / Tier 3: opus |
| QG-3 Stage2 Agent B | セキュリティレビュー（OWASP、認証認可） | `general-purpose` | opus（全Tier共通） |
| QG-4 | 根本原因分析 | `general-purpose` | Tier 1〜2: 修正3回目以降で opus / Tier 3: 修正1回目から opus |

### タスク複雑度シグナル（モデル選択の補助）

ルーティングテーブルで迷う場合は、以下のシグナルを参考にする:

| シグナル | 推奨モデル |
|---------|-----------|
| 1〜2ファイル、仕様明確、統合なし | haiku |
| 複数ファイル、統合あり、パターンマッチング必要 | sonnet |
| 設計判断必要、アーキテクチャ影響あり、広範なコードベース理解が必要 | opus |

---

## Decision Record（横断メカニズム）

フロー中に判断が必要になった場合、ユーザーに聞かず、Decision Recordを残して進む。

### 保存先

worktreeルートの `.claude/tmp/decisions/` ディレクトリに保存:
```
.claude/tmp/decisions/
  DR-001-scope-split.md
  DR-002-ec-strategy.md
  DR-003-ac-ambiguity.md
```

### フォーマット

```markdown
# DR-{番号}: {タイトル}

- **Phase**: {E1〜E4 or T1〜T5/QG}（{フェーズ名}）
- **日時**: {ISO 8601}
- **状況**: {何を判断する必要があったか}

## 選択肢

| # | 選択肢 | メリット | デメリット |
|---|--------|---------|----------|
| 1 | {案A} | {メリット} | {デメリット} |
| 2 | {案B} | {メリット} | {デメリット} |

## 決定

**選択: {番号}** — {選択した案}

## 理由

{なぜこの選択が最適か。コードベースの調査結果、既存パターンとの整合性、リスク分析を含む}
```

### 判断基準（AIが最適解を選ぶ指針）

1. **既存パターンとの整合性**: コードベースの既存実装に合わせる
2. **最小変更原則**: 変更量が少ない方を選ぶ
3. **安全側に倒す**: リスクが不明な場合は保守的な選択
4. **チケットのACに忠実**: 受け入れ条件に明記されていることを優先
5. **可逆性**: 後から変更しやすい選択を優先

---

# 入力判定

`$ARGUMENTS` にチケットID・URL・または作業内容テキストを渡す。**2軸**で判定する。

### 軸1: チケットタイプ（フロー選択）

| チケットタイプ | 判定基準 | アクション |
|---------------|---------|-----------|
| **Epic** | issue_type が Epic、または子チケット/サブタスクを持つ | **上位フロー E1 へ** |
| **User Story** | issue_type が Story/US、またはACが複数の独立機能を含む | **上位フロー E1 へ** |
| **実装チケット** | issue_type が Task/Sub-task、またはスコープが単一機能 | **下位フロー T1 へ** |
| **判定不能** | Decision Recordに記録し、安全側（上位フロー）で進む | **上位フロー E1 へ** |

> **⚠️ 下位フローの実行ルール（最重要）**
>
> 下位フロー T1〜T5 は**一気通貫で実行する**。T3（実装）完了後にユーザーに確認を求めてはならない。
> - T3完了 → **即座にQG（品質ゲート）を起動** → QG完了 → T5（Push確認）
> - 途中で「PR作りますか？」「次に進みますか？」と聞くのは**フロー違反**

### 軸2: 作業モード（新規 or 修正）

チケット内容・AC・関連コードから作業モードを判定し、フロー全体に引き継ぐ。

| 作業モード | 判定基準 | フローへの影響 |
|-----------|---------|--------------|
| **新規開発** | 対象機能がまだ存在しない。新しいエンドポイント/画面/ドメインの追加 | 通常のフロー実行 |
| **既存修正** | 対象機能のコードが既に存在する。バグ修正、仕様変更、リファクタリング、機能拡張 | 各ステップで**修正時の目的**に従って実行（下記参照） |

> **⚠️ アンチパターン: 「既に実装済みだからスキップ」**
>
> 修正タスクで以下の判断をしてはならない:
> - 「コードが既にある → E2（設計）は不要」→ **誤り。修正の影響波及を調査するためにE2は必須**
> - 「テストが既にある → 検証基準定義は不要」→ **誤り。修正後の期待動作を定義するためにT3内の検証基準定義は必須**
> - 「小さな変更 → QG（品質ゲート）を簡略化」→ **誤り。小さな変更こそ既存コードへの副作用リスクがある**
>
> **修正時こそフローの各ステップが重要になる。** 各ステップの修正時の具体的な目的は upper-flow.md / lower-flow.md に記載。

---

# フロー詳細（参照ファイル）

各フローの詳細手順は参照ファイルに分離されている。該当するフローに進む際に読み込むこと。

## 要求定義（E1 前の任意フェーズ）

Epic/US の要求が曖昧・未整理な場合、E1 の前に要求整理を行い、仕様を明確化してからフローに入る。要求が十分に明確な場合はスキップしてよい。

| 状況 | 使用スキル |
|------|-----------|
| 対話形式で要求を引き出したい（要求が不明確・口頭段階） | **`software-requirements`** スキルを呼び出す |
| チケットを渡してフォーマルな設計書を生成したい | **`sier-dev`** スキルを呼び出す（要件定義書〜内部設計書まで生成可） |

`sier-dev` で生成した `docs/01_requirements/要件定義書.md` は E1 の仕様書ソースとして扱う。

## 上位フロー（E1〜E4）

**→ `references/upper-flow.md` を読み込む**

Epic/USチケットから設計→チケット分割→実行ループまでの全手順。E&C（Expand and Contract）のチケット分割ルール・テンプレート・具体例も含む。旧E4（AC生成）はE1に統合、旧E5はE4に繰り上げ。

## 下位フロー（T1〜T5）

**→ `references/lower-flow.md` を読み込む**

個別の実装チケットに対するWorktree作成→実装→品質ゲート→PR作成の全手順。旧T3-preはT3に統合、旧T3b+T4+T5は「QG（品質ゲート）」サブエージェントに統合。Native Team ワーカーモード、SendMessage通信プロトコルも含む。

> **フロー実行の鉄則**: T3完了後、QGを経由せずにT5（PR作成）に進んではならない。T3→QG→T5は一気通貫。

## 最終報告書

**→ `references/final-report.md` を読み込む**

作業完了時に生成する最終報告書のテンプレートと生成ルール。上位フロー（E4完了後）・下位フロー単独（T5 Push確認前・PR作成前）の両方で必ず実行する。

## 横断的ルール（全フェーズ共通）

**→ `references/quality-rules.md` を読み込む**

Verification Before Completion 鉄則と Anti-rationalization パターン集。フロー実行開始時に読み込み、全フェーズで適用する。

## ドキュメント同期（SSOT）

**→ `references/doc-sync.md` を読み込む**

各フェーズ完了時に「今どの情報を根拠に作業しているか」を `scratch.md` に宣言するプロトコル。E1完了→`spec-draft.md`、E2完了→`design.md`、E3完了→`ticket-plan.md` の順にSSOTが移行する。

## サブエージェント通信プロトコル

**→ `references/subagent-protocol.md` を読み込む**

ワーカー↔Team Lead 間のステータス定義と処理フロー。E4（Native Team オーケストレーション）で使用する。

---

## 使用方法

```
# Epic/USチケット → 上位フロー（設計→分割→実装ループ）
/kouunryuusui EPIC-123
/kurs US-456

# 実装チケット → 下位フロー（直接実装）
/kouunryuusui APP-789
/kurs TASK-012
```
